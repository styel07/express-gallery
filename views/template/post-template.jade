//- iterates through all pictures in the image folder
each post, index in posts

  //- places each image in a div
  div.small-4.medium-4.large-4.columns
    a(href="#{}")
      img(src="#{post.image}")

  //- for every 3 images create a horizontal row
  if (index + 1) % 3 === 0
    hr